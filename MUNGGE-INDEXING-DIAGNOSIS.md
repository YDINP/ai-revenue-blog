# mungge 색인 진단

> 2026-07-30 · 도구: GSC URL 검사 API / Search Analytics / GA4 Data API / 직접 HTTP 점검
> 조회: `/api/gsc-sync?blog=mg&inspect=…`, `?dim=date&blog={mg,tf}`, `?ga4=probe&blog=mg`

## 결론

**기술적 결함은 없다.** 점검한 8개 항목 전부 정상이고, 301(308) 배포 **3일차**의
정상적인 이전 지연 구간이다. 다만 **1위 유입글 1편이 색인 공백에 빠져 있어** 이건 급하다.

## 1. 기술 점검 — 전부 통과

| 항목 | 결과 |
|---|---|
| mungge 사이트맵 | `sitemap.xml` → 하위 4개. 글 **194** + 페이지 38 = 232 URL |
| 사이트맵 제출 | 07-24 제출·다운로드 완료, 오류 0 / 경고 0 — **단, §1-1 참조(정상 아님)** |
| lastmod | 194건 **전부 존재**, 전부 2026-07 (최신) |
| robots.txt | 허용 (`Disallow`는 wp-admin·검색결과만) |
| canonical | **자기참조 정상** (샘플 3건 모두 mungge 자기 URL) |
| noindex / X-Robots-Tag | **없음** |
| 구→신 리다이렉트 | **308** 정상 동작 (Google은 301과 동등 처리) |
| 구 도메인 사이트맵 | `sitemap-index.xml` **200 접근 가능**, `/blog/` 184건 유지 |
| mungge 홈 | **색인됨** (크롤 2026-07-29) |

> 구 사이트맵이 살아 있는 건 **잘 되어 있는 것**이다. 구글이 이걸 크롤하며 308을
> 발견해야 이전이 진행된다. 이전 완료 전까지 내리지 말 것.

## 1-1. ⚠️ 정정 — 사이트맵은 "정상"이 아니다

GSC Sitemaps 화면 확인 결과, 위 표의 "오류 0"은 **API가 보여주지 못한 문제를 가리고 있었다.**

| Sitemap | 유형 | 제출 | 마지막 읽음 | 상태 | **발견된 페이지** |
|---|---|---|---|---|---|
| `/sitemap.xml` | Sitemap 색인 | 2026-07-24 | 2026-07-24 | 성공 | **0** |

**문제: 하위 사이트맵이 전개되지 않았다.** `/sitemap.xml`은 색인 파일이고 그 안에
하위 4개(post/page/category/post_tag)를 가리킨다. 정상이라면 구글이 색인을 처리한 뒤
하위 사이트맵들을 **각각 별도 행으로 추가**하고 페이지 수를 표시한다. 6일이 지나도록
행이 1개뿐이고 발견된 페이지가 0이다.

이것이 앞서 관측한 두 현상을 설명한다:
- 표본 10건 중 **5건이 "Google에 아직 알려지지 않은 URL"** — 사이트맵엔 있는데 발견 안 됨
- URL 검사 패널의 **"Sitemaps: 일시적인 처리 오류"**
- 발견 경로가 사이트맵이 아니라 **리다이렉트**로 잡힌 것

하위 사이트맵 자체는 멀쩡하다(직접 확인):

| 경로 | HTTP | Content-Type | URL 수 |
|---|---|---|---|
| `/sitemap-post-type-post.xml` | 200 | text/xml | **194** |
| `/sitemap-post-type-page.xml` | 200 | text/xml | 38 |
| `/sitemap-taxonomy-category.xml` | 200 | text/xml | 9 |
| `/sitemap-taxonomy-post_tag.xml` | 200 | text/xml | 28 |

**조치: 하위 사이트맵을 직접 제출한다.** 색인 파일 처리를 우회해 194개 글 URL을
곧바로 발견시키는 방법이다. GSC → Sitemaps → 새 사이트맵 추가에 아래를 각각 입력:

```
sitemap-post-type-post.xml     ← 글 194건, 가장 중요
sitemap-post-type-page.xml     ← 페이지 38건 (계산기 등)
sitemap-taxonomy-category.xml
sitemap-taxonomy-post_tag.xml
```

기존 `/sitemap.xml` 행은 지우지 말고 그대로 둔다(중복 제출은 무해).

## 2. "GSC 노출 0"은 버그가 아니다

겉보기 모순:

| 소스 | 값 (28일) |
|---|---|
| mungge **GSC** | 노출 **0** · 클릭 **0** (07-21~27 전일 0) |
| mungge **GA4** | google / organic **31 세션** |

**정상이다.** 검색결과에 걸려 있는 URL이 아직 **구 TF 주소**라, 클릭은 TF 속성에
계상되고 사용자만 308로 mungge에 착지한다. mungge URL 자체가 순위를 잡기 전까지
mungge GSC가 0인 게 맞다.

근거 — GA4 mungge 일별 세션이 **301 배포일(07-27)부터 급증**:

```
07-27: 35 세션   ← 301 배포
07-28: 45 세션
07-29: 29 세션
07-30: 16 세션 (진행 중)
```

28일 총 PV 163 중 대부분이 최근 4일에 몰려 있다. 리다이렉트가 실제로 작동하는 증거다.

한편 구 TF 도메인 클릭은 아직 안 꺾였다(07-27 13클릭, 최근 7일 84 vs 직전 7일 71).
단 GSC 데이터가 07-27까지라 **301 이후 효과는 아직 반영 전**이다.

## 3. 진짜 리스크 — 색인 공백

유입 상위 6편을 두 속성에서 대조:

| 글 | 구 TF | mungge | 판정 |
|---|---|---|---|
| **인디게임 수익 현실** (클릭 139) | 크롤O·색인X | 크롤O·색인X | **⚠️ 색인 공백** |
| 스팀 기대 신작 | 색인됨 | 크롤O·색인X | 정상 대기 |
| GPT-5.6 Codex | 색인됨 | 색인됨 | 중복(통합 대기) |
| 스팀 리뷰 판매량 추정 | 색인됨 | 크롤O·색인X | 정상 대기 |
| 스팀 넥스트 페스트 | 색인됨 | 크롤O·색인X | 정상 대기 |
| 인디게임 광고 수익 | 색인됨 | 크롤O·색인X | 정상 대기 |

**1위 글이 양쪽 다 색인에서 빠져 있다.** 구 URL은 07-29 크롤 후 드롭됐고,
mungge 버전은 아직 색인 전이다. 이 글이 사이트 전체 클릭의 **67.5%**라,
공백이 길어지면 유입 3분의 2가 사라진다.

또한 표본 10건 중 **5건은 "Google에 아직 알려지지 않은 URL"** — 사이트맵에는
들어 있으나(확인함) 크롤이 아직 안 왔다. 구글이 사이트맵을 마지막으로 내려간 게
**07-24**이고 그 뒤 재다운로드가 없다.

## 4. 처방 (우선순위)

1. **1위 글 mungge URL 색인 생성 요청** — GSC UI에서 URL 검사 → "색인 생성 요청".
   API로는 불가(수동). 공백 상태라 가장 급함.
   `https://mungge.com/2026-02-26-game-2026-indie-game-monetization-reality-5-truths/`
2. **주소 변경 도구(Change of Address) 검토** — TF 속성 설정 → 주소 변경 → mungge 지정.
   양쪽 소유확인·전면 리다이렉트 조건을 이미 충족한다. 이전 속도를 가장 크게 올리는 수단.
3. **mungge 사이트맵 재제출** — 마지막 다운로드가 07-24. 재제출로 재크롤 유도.
4. **구 도메인 사이트맵·robots 유지** — 지금 상태가 맞다. 건드리지 말 것.
5. **2주 관찰** — 지표는 ① mungge GSC 노출이 0을 벗어나는가 ② "미발견" 비율 감소
   ③ 1위 글 색인 복귀. `scripts/naver-index-check.mjs`는 네이버용이라 이 건엔 안 쓴다.

## 4-1. 조치 결과 (2026-07-30 당일 추가)

| 조치 | 상태 |
|---|---|
| **주소 변경 도구** | ✅ **활성** — mungge.com ← ai-revenue-blog.vercel.app 이전 진행 중 |
| 1위 글 색인 생성 요청 | ⚠️ 실패 — "제출 중 문제가 발생했습니다"(구글측 일시 오류 또는 일일 할당량 소진) |
| 사이트맵 재제출(API) | ❌ 불가 — 서비스 계정 스코프가 `webmasters.readonly` (`api/_gsc.js:12`). UI로만 가능 |

**주소 변경 도구가 켜졌으므로 개별 색인 요청의 가치는 크게 떨어진다.** 이 도구는
구 사이트의 순위 신호를 신 사이트로 넘기고 재크롤을 가속하는 게 본래 기능이라,
글 단위로 밀어 올리는 것보다 상위 수단이다. 색인 요청 실패는 **재시도 루프를 돌 일이 아니다**
(내일 1회만 재시도, 안 되면 그대로 둔다).

### URL 검사 패널에서 추가로 확인된 것

- **Google에서 선택한 표준 URL = 검사된 URL** → 구글이 **mungge를 정본으로 채택**했다.
  중복·canonical 충돌이 없다는 뜻이라 남은 건 색인 등재뿐이다. 긍정 신호.
- 참조 페이지 = 구 TF URL → 발견 경로가 **사이트맵이 아니라 리다이렉트**다.
  구 사이트맵·robots를 유지해야 하는 이유가 여기서 다시 확인된다.
- 1위 글의 사이트맵 `lastmod`가 **2026-07-30T10:15:21** (오늘 내부링크 삽입 시각)으로
  갱신됐다. 구글 입장에선 방금 수정된 문서라 재크롤 우선순위가 올라간다.
- 패널의 "Sitemaps: 일시적인 처리 오류"는 사이트맵 자체 문제가 아니다.
  실제 사이트맵에 해당 URL이 정상 등재돼 있음을 직접 확인했다(194건 중 1건 일치).

## 5. 판단

색인이 안 되는 게 아니라 **아직 안 된 것**이다. 3일은 이르다.
지금 할 일은 구조를 고치는 게 아니라 **공백에 빠진 1편을 수동으로 끌어올리고
이전 속도를 높이는 것**이다. 2주 뒤에도 mungge 노출이 0이면 그때 구조를 다시 본다.

## 미확인

- 구 TF 사이트맵은 `isPending: true`(07-28 제출)이고 마지막 다운로드가 2026-02-26이다.
  구글이 이 사이트맵을 5개월째 안 내려갔다는 뜻이라, 리다이렉트 발견이 느릴 수 있다.
  다만 URL 검사에서 발견 경로가 리다이렉트로 확인됐고 주소 변경 도구도 켜졌으므로
  실질 영향은 제한적으로 본다.

## 재점검 방법 (2주 뒤)

```bash
S=<CRON_SECRET>; B=https://ai-revenue-blog.vercel.app/api/gsc-sync
# ① mungge 노출이 0을 벗어났는가 (가장 중요)
curl -s "$B?dim=date&blog=mg&days=28&secret=$S"
# ② 1위 글 색인 복귀 여부
curl -s "$B?blog=mg&inspect=/2026-02-26-game-2026-indie-game-monetization-reality-5-truths/&secret=$S"
# ③ 구 도메인 클릭 감소분이 mungge로 넘어왔는가
curl -s "$B?dim=date&blog=tf&days=28&secret=$S"
curl -s "$B?ga4=probe&blog=mg&days=28&dim=date&secret=$S"
```

판정 기준 — ①이 0을 벗어나면 이전 성공. 2주 뒤에도 0이고 ③에서 TF 클릭만 줄었다면
그때는 이전 실패로 보고 구조를 다시 파헤친다.
