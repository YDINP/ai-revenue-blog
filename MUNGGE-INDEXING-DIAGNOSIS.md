# mungge 색인 진단

> 2026-07-30 · 도구: GSC URL 검사 API / Search Analytics / GA4 Data API / 직접 HTTP 점검
> 조회: `/api/gsc-sync?blog=mg&inspect=…`, `?dim=date&blog={mg,tf}`, `?ga4=probe&blog=mg`

## 결론

**기술적 결함은 없다.** 점검한 8개 항목 전부 정상이고, 301(308) 배포 **3일차**의
정상적인 이전 지연 구간이다. 다만 **1위 유입글 1편이 색인 공백에 빠져 있어** 이건 급하다.

## 1. 기술 점검 — 전부 통과

| 항목 | 결과 |
|---|---|
| mungge 사이트맵 | `sitemap.xml` → 하위 4개. 글 **194** + 페이지 38 = 232 URL |
| 사이트맵 제출 | 07-24 제출·다운로드 완료, **오류 0 / 경고 0** |
| lastmod | 194건 **전부 존재**, 전부 2026-07 (최신) |
| robots.txt | 허용 (`Disallow`는 wp-admin·검색결과만) |
| canonical | **자기참조 정상** (샘플 3건 모두 mungge 자기 URL) |
| noindex / X-Robots-Tag | **없음** |
| 구→신 리다이렉트 | **308** 정상 동작 (Google은 301과 동등 처리) |
| 구 도메인 사이트맵 | `sitemap-index.xml` **200 접근 가능**, `/blog/` 184건 유지 |
| mungge 홈 | **색인됨** (크롤 2026-07-29) |

> 구 사이트맵이 살아 있는 건 **잘 되어 있는 것**이다. 구글이 이걸 크롤하며 308을
> 발견해야 이전이 진행된다. 이전 완료 전까지 내리지 말 것.

## 2. "GSC 노출 0"은 버그가 아니다

겉보기 모순:

| 소스 | 값 (28일) |
|---|---|
| mungge **GSC** | 노출 **0** · 클릭 **0** (07-21~27 전일 0) |
| mungge **GA4** | google / organic **31 세션** |

**정상이다.** 검색결과에 걸려 있는 URL이 아직 **구 TF 주소**라, 클릭은 TF 속성에
계상되고 사용자만 308로 mungge에 착지한다. mungge URL 자체가 순위를 잡기 전까지
mungge GSC가 0인 게 맞다.

근거 — GA4 mungge 일별 세션이 **301 배포일(07-27)부터 급증**:

```
07-27: 35 세션   ← 301 배포
07-28: 45 세션
07-29: 29 세션
07-30: 16 세션 (진행 중)
```

28일 총 PV 163 중 대부분이 최근 4일에 몰려 있다. 리다이렉트가 실제로 작동하는 증거다.

한편 구 TF 도메인 클릭은 아직 안 꺾였다(07-27 13클릭, 최근 7일 84 vs 직전 7일 71).
단 GSC 데이터가 07-27까지라 **301 이후 효과는 아직 반영 전**이다.

## 3. 진짜 리스크 — 색인 공백

유입 상위 6편을 두 속성에서 대조:

| 글 | 구 TF | mungge | 판정 |
|---|---|---|---|
| **인디게임 수익 현실** (클릭 139) | 크롤O·색인X | 크롤O·색인X | **⚠️ 색인 공백** |
| 스팀 기대 신작 | 색인됨 | 크롤O·색인X | 정상 대기 |
| GPT-5.6 Codex | 색인됨 | 색인됨 | 중복(통합 대기) |
| 스팀 리뷰 판매량 추정 | 색인됨 | 크롤O·색인X | 정상 대기 |
| 스팀 넥스트 페스트 | 색인됨 | 크롤O·색인X | 정상 대기 |
| 인디게임 광고 수익 | 색인됨 | 크롤O·색인X | 정상 대기 |

**1위 글이 양쪽 다 색인에서 빠져 있다.** 구 URL은 07-29 크롤 후 드롭됐고,
mungge 버전은 아직 색인 전이다. 이 글이 사이트 전체 클릭의 **67.5%**라,
공백이 길어지면 유입 3분의 2가 사라진다.

또한 표본 10건 중 **5건은 "Google에 아직 알려지지 않은 URL"** — 사이트맵에는
들어 있으나(확인함) 크롤이 아직 안 왔다. 구글이 사이트맵을 마지막으로 내려간 게
**07-24**이고 그 뒤 재다운로드가 없다.

## 4. 처방 (우선순위)

1. **1위 글 mungge URL 색인 생성 요청** — GSC UI에서 URL 검사 → "색인 생성 요청".
   API로는 불가(수동). 공백 상태라 가장 급함.
   `https://mungge.com/2026-02-26-game-2026-indie-game-monetization-reality-5-truths/`
2. **주소 변경 도구(Change of Address) 검토** — TF 속성 설정 → 주소 변경 → mungge 지정.
   양쪽 소유확인·전면 리다이렉트 조건을 이미 충족한다. 이전 속도를 가장 크게 올리는 수단.
3. **mungge 사이트맵 재제출** — 마지막 다운로드가 07-24. 재제출로 재크롤 유도.
4. **구 도메인 사이트맵·robots 유지** — 지금 상태가 맞다. 건드리지 말 것.
5. **2주 관찰** — 지표는 ① mungge GSC 노출이 0을 벗어나는가 ② "미발견" 비율 감소
   ③ 1위 글 색인 복귀. `scripts/naver-index-check.mjs`는 네이버용이라 이 건엔 안 쓴다.

## 5. 판단

색인이 안 되는 게 아니라 **아직 안 된 것**이다. 3일은 이르다.
지금 할 일은 구조를 고치는 게 아니라 **공백에 빠진 1편을 수동으로 끌어올리고
이전 속도를 높이는 것**이다. 2주 뒤에도 mungge 노출이 0이면 그때 구조를 다시 본다.

## 미확인

- 주소 변경 도구가 `*.vercel.app` 서브도메인 속성에서 제공되는지는 UI에서 확인 필요
  (API로 조회 불가). 안 되면 1·3번만으로 진행.
- 구 TF 사이트맵은 `isPending: true`(07-28 제출)이고 마지막 다운로드가 2026-02-26이다.
  구글이 이 사이트맵을 5개월째 안 내려갔다는 뜻이라, 리다이렉트 발견이 느릴 수 있다.
