---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = 'Contact';
const description = 'TechFlow 블로그에 문의하거나 협업을 제안하고 싶으신가요? 이메일 또는 문의 양식을 통해 연락 주세요.';
---

<BaseLayout title={title} description={description}>
  <article class="blog-post contact-page">
    <header class="post-header">
      <h1>Contact</h1>
      <p class="tagline">문의, 제안, 협업은 언제나 환영합니다!</p>
    </header>

    <div class="post-content">
      <section class="intro-section">
        <p>
          TechFlow에 관심을 가져주셔서 감사합니다.
          기술 리뷰 협업, 기고 제안, 광고 문의, 기타 질문이 있으시면 아래 방법으로 연락 주세요.
        </p>
      </section>

      <div class="contact-grid">
        <section class="contact-card">
          <h2>📧 이메일</h2>
          <p>가장 빠른 응답을 받으실 수 있는 방법입니다.</p>
          <a href="mailto:techflow.blog@gmail.com" class="contact-link">techflow.blog@gmail.com</a>
          <p class="note">영업일 기준 1-2일 내 답변 드립니다.</p>
        </section>

        <section class="contact-card">
          <h2>🐙 GitHub</h2>
          <p>오픈소스 프로젝트 및 기술 문의</p>
          <a href="https://github.com/YDINP" target="_blank" rel="noopener" class="contact-link">github.com/YDINP</a>
          <p class="note">코드 리뷰 및 기술 토론 환영합니다.</p>
        </section>

        <section class="contact-card">
          <h2>🌐 웹사이트</h2>
          <p>최신 콘텐츠 및 블로그 정보</p>
          <a href="https://ai-revenue-blog.vercel.app" target="_blank" rel="noopener" class="contact-link">ai-revenue-blog.vercel.app</a>
          <p class="note">RSS 피드도 제공합니다.</p>
        </section>
      </div>

      <section class="form-section">
        <h2>문의 양식</h2>
        <p class="form-intro">
          아래 양식을 작성하시거나, 직접 이메일(<a href="mailto:techflow.blog@gmail.com">techflow.blog@gmail.com</a>)로 보내주셔도 됩니다.
        </p>

        <form class="contact-form" action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
          <div class="form-group">
            <label for="name">이름 *</label>
            <input type="text" id="name" name="name" required placeholder="홍길동">
          </div>

          <div class="form-group">
            <label for="email">이메일 *</label>
            <input type="email" id="email" name="email" required placeholder="example@email.com">
          </div>

          <div class="form-group">
            <label for="subject">제목 *</label>
            <input type="text" id="subject" name="subject" required placeholder="문의 제목을 입력하세요">
          </div>

          <div class="form-group">
            <label for="category">문의 유형</label>
            <select id="category" name="category">
              <option value="general">일반 문의</option>
              <option value="collaboration">협업 제안</option>
              <option value="review">제품 리뷰 요청</option>
              <option value="advertisement">광고 문의</option>
              <option value="technical">기술 문의</option>
              <option value="other">기타</option>
            </select>
          </div>

          <div class="form-group">
            <label for="message">메시지 *</label>
            <textarea id="message" name="message" rows="8" required placeholder="문의 내용을 상세히 작성해 주세요."></textarea>
          </div>

          <button type="submit" class="btn-submit">전송하기</button>

          <p class="form-note">
            * 표시가 있는 항목은 필수 입력 사항입니다.<br>
            전송하신 정보는 개인정보 처리방침에 따라 안전하게 처리됩니다.
          </p>
        </form>
      </section>

      <section class="collaboration-section">
        <h2>협업 및 기고 안내</h2>

        <div class="collaboration-grid">
          <div class="collab-card">
            <h3>🤝 제품 리뷰 협업</h3>
            <p>개발 도구, SaaS 서비스, 하드웨어 등의 제품 리뷰를 진행합니다.</p>
            <ul>
              <li>공정하고 객관적인 리뷰 보장</li>
              <li>실사용 기반 상세 분석</li>
              <li>벤치마크 및 비교 테스트 포함</li>
            </ul>
          </div>

          <div class="collab-card">
            <h3>✍️ 게스트 포스트</h3>
            <p>기술 전문가의 게스트 포스트를 환영합니다.</p>
            <ul>
              <li>AI, 개발, 기술 트렌드 관련 주제</li>
              <li>고품질 콘텐츠 우선</li>
              <li>저자 프로필 및 링크 제공</li>
            </ul>
          </div>

          <div class="collab-card">
            <h3>📢 광고 및 스폰서십</h3>
            <p>블로그 광고 및 스폰서 콘텐츠를 제공합니다.</p>
            <ul>
              <li>Google AdSense 디스플레이 광고</li>
              <li>스폰서 포스트 (명확한 공시)</li>
              <li>제휴 마케팅 파트너십</li>
            </ul>
          </div>
        </div>
      </section>

      <section class="faq-section">
        <h2>자주 묻는 질문</h2>

        <div class="faq-item">
          <h3>Q. 답변은 얼마나 걸리나요?</h3>
          <p>A. 영업일 기준 1-2일 내에 답변 드립니다. 복잡한 문의는 더 오래 걸릴 수 있습니다.</p>
        </div>

        <div class="faq-item">
          <h3>Q. 제품 리뷰는 유료인가요?</h3>
          <p>A. 제품 샘플 제공 시 무료 리뷰가 가능하며, 스폰서 리뷰는 별도 협의합니다. 모든 리뷰는 객관적으로 작성됩니다.</p>
        </div>

        <div class="faq-item">
          <h3>Q. 게스트 포스트는 어떻게 제출하나요?</h3>
          <p>A. 먼저 이메일로 주제와 간단한 개요를 보내주시면, 검토 후 안내 드립니다.</p>
        </div>

        <div class="faq-item">
          <h3>Q. 광고 문의는 어떻게 하나요?</h3>
          <p>A. 이메일로 광고 형태, 예산, 기간 등을 보내주시면 맞춤 제안서를 드립니다.</p>
        </div>
      </section>

      <section class="social-section">
        <h2>소셜 미디어</h2>
        <p>TechFlow의 최신 소식을 받아보세요!</p>
        <div class="social-links">
          <a href="/rss.xml" class="social-btn" target="_blank">
            📡 RSS 피드
          </a>
          <a href="https://github.com/YDINP" class="social-btn" target="_blank" rel="noopener">
            🐙 GitHub
          </a>
        </div>
      </section>
    </div>
  </article>
</BaseLayout>

<style>
  .contact-page {
    max-width: 800px;
    margin: 0 auto;
  }

  .post-header {
    margin-bottom: 2.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 2px solid var(--color-border);
    text-align: center;
  }

  .post-header h1 {
    font-size: 2.5rem;
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: 0.75rem;
  }

  .tagline {
    font-size: 1.15rem;
    color: var(--color-text-secondary);
    font-weight: 500;
  }

  .intro-section p {
    font-size: 1.05rem;
    line-height: 1.8;
    color: var(--color-text);
    text-align: center;
    margin-bottom: 2rem;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0 3rem;
  }

  .contact-card {
    padding: 1.75rem;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    text-align: center;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .contact-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(37, 99, 235, 0.12);
  }

  .contact-card h2 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-text);
    border: none;
    padding: 0;
  }

  .contact-card p {
    color: var(--color-text-secondary);
    font-size: 0.9rem;
    margin-bottom: 0.75rem;
  }

  .contact-link {
    display: inline-block;
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 600;
    font-size: 1.05rem;
    margin: 0.5rem 0;
    word-break: break-all;
  }

  .contact-link:hover {
    text-decoration: underline;
  }

  .note {
    font-size: 0.8rem !important;
    color: var(--color-text-secondary) !important;
    font-style: italic;
  }

  .form-section {
    margin: 3rem 0;
    padding: 2rem;
    background: var(--color-bg-secondary);
    border-radius: 16px;
    border: 1px solid var(--color-border);
  }

  .form-section h2 {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1.75rem;
    color: var(--color-text);
  }

  .form-intro {
    margin-bottom: 1.5rem;
    color: var(--color-text-secondary);
  }

  .form-intro a {
    color: var(--color-primary);
    text-decoration: underline;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-group label {
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--color-text);
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    font-size: 1rem;
    font-family: inherit;
    background: var(--color-bg);
    color: var(--color-text);
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 150px;
  }

  .btn-submit {
    padding: 0.875rem 2rem;
    background: var(--color-primary);
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1.05rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    margin-top: 0.5rem;
  }

  .btn-submit:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
  }

  .form-note {
    font-size: 0.8rem;
    color: var(--color-text-secondary);
    text-align: center;
    margin-top: 0.5rem;
  }

  .collaboration-section {
    margin: 3rem 0;
  }

  .collaboration-section h2 {
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
  }

  .collaboration-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.25rem;
  }

  .collab-card {
    padding: 1.5rem;
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    border-left: 4px solid var(--color-primary);
  }

  .collab-card h3 {
    font-size: 1.15rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .collab-card p {
    color: var(--color-text-secondary);
    font-size: 0.9rem;
    margin-bottom: 1rem;
  }

  .collab-card ul {
    list-style: none;
    padding-left: 0;
  }

  .collab-card li {
    padding-left: 1.25rem;
    position: relative;
    margin-bottom: 0.5rem;
    color: var(--color-text);
    font-size: 0.85rem;
  }

  .collab-card li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: bold;
  }

  .faq-section {
    margin: 3rem 0;
  }

  .faq-section h2 {
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
  }

  .faq-item {
    margin-bottom: 1.5rem;
    padding: 1.25rem;
    background: var(--color-bg-secondary);
    border-radius: 10px;
    border-left: 4px solid var(--color-accent);
  }

  .faq-item h3 {
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  .faq-item p {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    margin: 0;
  }

  .social-section {
    margin: 3rem 0;
    text-align: center;
    padding: 2rem;
    background: linear-gradient(135deg, var(--color-bg-secondary), var(--color-bg));
    border-radius: 16px;
    border: 1px solid var(--color-border);
  }

  .social-section h2 {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .social-section p {
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
  }

  .social-links {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .social-btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: var(--color-primary);
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.2s;
  }

  .social-btn:hover {
    background: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
  }

  @media (max-width: 640px) {
    .post-header h1 {
      font-size: 2rem;
    }

    .tagline {
      font-size: 1rem;
    }

    .contact-grid {
      grid-template-columns: 1fr;
    }

    .form-section {
      padding: 1.5rem;
    }

    .collaboration-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
