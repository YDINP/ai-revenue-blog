# Blog Content Guidelines (블로그 콘텐츠 작성 가이드)

## 필수 규칙

### 0. 최신 데이터 기반 작성 (최우선 원칙)

모든 포스트는 **검증 가능한 최신 데이터**를 기반으로 작성해야 합니다. 정보 신뢰도가 블로그 품질의 핵심입니다.

- **실제 수치만 사용**: 벤치마크(SWE-bench, Terminal-Bench 등), 가격, 출시일은 공식 발표 기준
- **허구 데이터 금지**: 존재하지 않는 제품명, 가짜 통계, 임의의 퍼센트 사용 금지
- **불확실한 정보 표기**: 확인 불가한 수치는 "공식 발표 예정", "추정치" 등으로 명시
- **비교 글 필수 검증**: 경쟁 제품 비교 시 양쪽 모두 최신 공식 데이터 사용
- **시의성**: 6개월 이상 된 데이터는 "YYYY년 M월 기준" 명시

### 1. 시각 차트 (비교/분석 글 필수)

비교, 분석, 리뷰 성격의 글에는 **반드시** 시각 차트를 포함해야 합니다.

#### chart-bar (막대 차트)
수치 데이터 비교에 사용:
```html
<div class="chart-bar"
  data-title="차트 제목"
  data-labels="항목1,항목2,항목3"
  data-values="85,92,78"
  data-colors="#10b981,#3b82f6,#f59e0b"
  data-unit="점">
</div>
```

#### chart-radar (항목별 점수 비교)
여러 항목을 점수로 비교할 때 사용 (10점 만점):
```html
<div class="chart-radar"
  data-title="차트 제목"
  data-items='[
    {"name":"제품A","scores":[
      {"label":"성능","value":9,"color":"#10b981"},
      {"label":"가격","value":7,"color":"#10b981"}
    ]},
    {"name":"제품B","scores":[
      {"label":"성능","value":8,"color":"#3b82f6"},
      {"label":"가격","value":9,"color":"#3b82f6"}
    ]}
  ]'>
</div>
```

#### 색상 팔레트
- 제품/모델 A: `#10b981` (초록)
- 제품/모델 B: `#3b82f6` (파랑)
- 제품/모델 C: `#f59e0b` (노랑)
- 제품/모델 D: `#ef4444` (빨강)
- 제품/모델 E: `#8b5cf6` (보라)
- 비강조/참고: `#94a3b8` (회색)

### 2. 차트 배치 위치
- **종합 비교 차트**: 첫 번째 비교 테이블 바로 아래 또는 본문 시작 직후
- **세부 비교 차트**: 해당 섹션의 비교 테이블 바로 아래
- **결론 차트**: 최종 추천 섹션 직전

### 3. 차트가 필요한 글 유형
- "vs", "비교", "추천", "TOP", "순위" 키워드가 포함된 글
- 2개 이상의 제품/서비스/도구를 비교하는 글
- 수치 데이터(가격, 성능, 점수)가 포함된 글

### 3.4. 차트 정렬·유형 선택 (데이터 성격에 맞출 것)

차트는 **데이터의 성격에 맞는 유형**을 고르고, 막대는 **정렬**한다.

- **정렬 필수**: `chart-bar`의 항목은 값 기준 내림차순(또는 오름차순)으로 정렬한다. "낮을수록 좋음"인 지표(반응속도·빌드시간·가격 등)는 오름차순 + `data-title`에 "(낮을수록 좋음)" 명시. 순서에 의미가 있는 경우(단계·시간순)만 예외.
- **유형 매칭**:
  - 랭킹/수치 비교 → `chart-bar` (정렬)
  - 구성비/점유율(합=100%) → `chart-donut`
  - **분포(전체를 구간으로 나눔)** → `chart-bar`로 **상호배타 구간**만, 합이 100%가 되게. 겹치는 항목(예: "상위 8.5%"와 "리뷰 1000개 3%"는 부분집합)을 한 분포에 섞지 말 것.
  - 다속성 종합 평가 → `chart-radar`
  - 1:1 맞대결 → `chart-versus`
  - 단일 지표 진행/정도 → `chart-progress`
- **가로 vs 세로**: 비교형 `chart-bar`는 라벨이 짧으면(각 8자 이하·항목 2~6개) `data-orient="vertical"`(세로 막대)로 넣는다 — 비교 가독성이 더 좋다. 라벨이 길거나(제품명·모델명 등) 항목이 7개 이상이면 가로(기본)로 둔다.
- **금지**: 서로 배타적이지 않은 항목을 한 "분포/비율" 차트에 넣기, 정렬 안 된 뒤죽박죽 막대, 데이터 성격과 안 맞는 유형(예: 시계열을 도넛으로).

### 3.5. 강조 표기 (볼드 남용 금지 — AI 티 제거)

문장 곳곳에 `**볼드**`를 균일하게 뿌리는 것은 전형적인 AI 생성 티다. **금지**한다.

- **기본은 평문**: 대부분의 강조 문구는 볼드 없이 평문으로 둔다.
- **선택적 강조만**: 정말 핵심인 지점(대표 수치·핵심 주장) 몇 곳에만 강조를 준다. 한 섹션에 강조 1~2개 이내를 목표로 한다.
- **강조 수단을 섞는다**: 볼드 하나로 통일하지 말고, 대표 수치는 폰트 크기로 키운다.
  ```html
  중앙값은 <span style="font-size:1.35em;font-weight:800">약 249달러</span>다.
  ```
  - 히어로 수치(글의 핵심 숫자): `font-size:1.2~1.35em; font-weight:800`
  - 보조 강조: `font-size:1.1~1.15em; font-weight:700` 또는 일반 `**볼드**`
- **라벨성 볼드는 허용**: 목록의 용어 정의(`**프리미엄** — …`), 단계 리드인(`**첫째, …**`)처럼 구조적 라벨은 볼드 유지 가능.
- **금지 패턴**: 한 문단에서 서술 문구를 3개 이상 볼드 처리, 링크 텍스트 통째로 볼드, 문장 전체 볼드.

### 3.6. URL(슬러그) 규칙 — 날짜 금지

포스트 URL은 `/blog/<파일명>/` 형태로 **파일명이 곧 URL 슬러그**다. URL에 날짜를 넣지 않는다.

- **새 글 파일명에서 `YYYY-MM-DD-` 날짜 prefix를 붙이지 말 것.** 주제 슬러그만 사용.
  - ❌ `2026-07-20-game-indie-marketing-guide.md` → `/blog/2026-07-20-game-indie-marketing-guide/`
  - ✅ `game-indie-marketing-guide.md` → `/blog/game-indie-marketing-guide/`
- **정렬·표시 날짜는 파일명이 아니라 frontmatter `pubDate`가 담당**한다(코드가 파일명에서 날짜를 파싱하지 않음). 파일명에 날짜가 없어도 정렬·내부링크·OG 전부 정상.
- 슬러그는 영문 소문자+하이픈, 카테고리 접두(`game-`/`ai-`/`dev-`/`review-`)는 유지 권장(기존 관례).
- ⚠️ **기존 155개 글은 이미 날짜 포함 URL로 네이버/구글 색인됨 → 소급 변경 금지**(색인·유입 리셋). 이 규칙은 신규 글에만 적용.

### 4. 이미지
- 모든 글에 `image` frontmatter 필수 (Pexels API large2x URL)
- `alt` 텍스트 한국어 작성

### 5. 쿠팡 링크
- 리뷰/추천 글에 관련 쿠팡 상품 1-3개 연결
- `coupangLinks` frontmatter 배열 사용

## n8n 자동 생성 시 체크리스트
- [ ] image frontmatter 포함?
- [ ] 비교 글이면 chart-bar 또는 chart-radar 포함?
- [ ] **chart-bar 항목이 값 기준으로 정렬됐는가? (§3.4)**
- [ ] **차트 유형이 데이터 성격에 맞는가? 분포에 겹치는 항목 없는가? (§3.4)**
- [ ] **`**볼드**`를 남발하지 않았는가? 강조는 핵심 몇 곳만+폰트크기 혼합? (§3.5)**
- [ ] **파일명에 날짜 prefix(`YYYY-MM-DD-`)가 없는가? URL에 날짜 금지 (§3.6)**
- [ ] coupangLinks 매핑?
- [ ] category/tags 정확?
- [ ] pubDate가 오늘 이하?
